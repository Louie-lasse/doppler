# Doppler

Pulse noises at randomized intervals

Place files you want to be included in `soundboard/`.

## Usage

The program can be started with or without predetermined arguments. Any number of arguments can be supplied beforehand, assuming correct order.

```sh
python main.py [soundfile.wav] [per_hour_frequency] [randomness]
```